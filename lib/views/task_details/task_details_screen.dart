import "package:flutter/material.dart";

class TaskDetailsScreen extends StatelessWidget {
  const TaskDetailsScreen({super.key});

  @override
  Widget build(final BuildContext context) =>
      const Center(child: Text("task details"));
}
